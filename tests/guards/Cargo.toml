[package]
name = "kwb-source-guards"
version = "0.1.0"
description = "Reading a crate's own source, for the tests that assert a structural property of it."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true
