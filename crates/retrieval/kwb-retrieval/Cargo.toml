[package]
name = "kwb-retrieval"
version = "0.1.0"
description = "Search and graph traversal over the domain model: keyword, semantic, hybrid, and neighbourhood queries."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
kwb-domain = { workspace = true }

[lints]
workspace = true
