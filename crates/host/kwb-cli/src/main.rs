//! The `kwb` binary. Composition root only — no domain logic lives here.
//!
//! Nothing is wired yet. This exists so the workspace has a real, buildable host from
//! the first commit rather than one added once something needs it.

fn main()
{
    println!("kwb: no verbs are wired yet");
}
